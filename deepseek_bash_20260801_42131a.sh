#!/bin/bash

# Create directory structure
mkdir -p solitaire-finz-mart-backend/{models,routes,controllers,middleware,config,scripts,uploads}

# Create all files (paste the content for each file)
# ... (you would need to echo the content into each file)

echo "Setup complete! Run: cd solitaire-finz-mart-backend && npm install"