# Create the directory structure
mkdir -p solitaire-finz-mart-backend/{models,routes,controllers,middleware,config,scripts,uploads}

# Then copy all files to their locations
# Finally, create the zip
zip -r solitaire-finz-mart-backend.zip solitaire-finz-mart-backend/